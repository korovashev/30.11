ssh-keygen -t ed25519 (или название своё) -C "email гита"
ls -alt
cd .ssh
cat (название).pub 

скопировать всё и вставить в основные настройки гита в ssh keys 

после этого всё будет подключено и остаётся только создать публичный репозиторий и туда во вкладке ssh keys вставить ключ из настроек гита

и можно использовать git clone!!!
